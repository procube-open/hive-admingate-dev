# クラス定義

本システムで ID Manager に定義されているクラスについて説明します。

## 作業期間(Period)
このクラスは内包オブジェクトです。
このクラスの属性は以下のとおりです。
### 期間開始日(validFrom)
| データ型|date|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 期間終了日(validUntil)
| データ型|date|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 時間指定(specificTime)
時間指定(specificTime) はこの項目をチェックすることで、作業可能な時間を指定することができます。です。
| データ型|boolean|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 開始時(startHour)
| データ型|number|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 開始分(startMinute)
| データ型|number|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 終了時(endHour)
| データ型|number|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 終了分(endMinute)
| データ型|number|
|----|----------------|
|必須|:o:|
|一意|:x:|
## 連携項目(alignment)
クラスのキー属性はid です。
このクラスの属性は以下のとおりです。
### 連携ID(id)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 連携日時(date)
| データ型|datetime|
|----|----------------|
|必須|:x:|
|一意|:x:|
## ブラウザ入力(browserStep)
このクラスは内包オブジェクトです。
このクラスの属性は以下のとおりです。
### タイプ(type)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 検索方法(by)
検索方法(by) は要素を探す方法です。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 指定値(value)
指定値(value) は検索方法で選んだ方法に対応する指定文字列
セレクタや属性値を入力です。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### タイムアウト(秒)(timeout_sec)
| データ型|number|
|----|----------------|
|必須|:x:|
|一意|:x:|
### URL(url)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### テキスト(text)
テキスト(text) は入力テキストです。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 入力前消去(clear)
入力前消去(clear) はinputの前に対象要素の中身を削除します。です。
| データ型|boolean|
|----|----------------|
|必須|:x:|
|一意|:x:|
### スリープ時間(ミリ秒)(ms)
| データ型|number|
|----|----------------|
|必須|:x:|
|一意|:x:|
## 接続先(connection)
クラスのキー属性はid です。
このクラスの属性は以下のとおりです。
### ID(id)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:o:|
### ホスト名(hostname)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 接続仕様(connectionSpecification)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 接続仕様名(connectionSpecificationName)
接続仕様名(connectionSpecificationName) は接続仕様選択時の表示名です。
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 参照のみ可能チーム(viewTeam)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 参照/編集可能チーム(editTeam)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 補足(remark)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### パスワード更新日時(changePasswordDate)
| データ型|datetime|
|----|----------------|
|必須|:x:|
|一意|:x:|
### SWCゾーン(swcZone)
SWCゾーン(swcZone) はsocket warp connector のクライアントIDです。
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
## 接続先グループ(connectionGroup)
クラスのキー属性はname です。
このクラスの属性は以下のとおりです。
### 接続先グループ名(name)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:o:|
### 接続先一覧(connections)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 所属チーム(team)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
## 接続仕様(connectionSpecification)
クラスのキー属性はid です。
このクラスの属性は以下のとおりです。
### ID(id)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 接続仕様名(name)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### パスワード更新用ID(ansibleID)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### パスワード更新用パスワード(ansiblePassword)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### OS種別(osType)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### プロトコル(protocol)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### ポート(port)
| データ型|number|
|----|----------------|
|必須|:x:|
|一意|:x:|
### ユーザー名(username)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### パスワード(password)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### WEB-SFTP有効化(web_sftp_enabled)
| データ型|boolean|
|----|----------------|
|必須|:x:|
|一意|:x:|
### ブラウザ認証方式(browser_auth_mode)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 特権ユーザー名(privileged_username)
特権ユーザー名(privileged_username) は認証方式が Basic/Digest の場合のユーザー名
自動入力の場合は ${privileged_username} を参照することでこれが入力できるです。
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 特権ユーザーパスワード(privileged_password)
特権ユーザーパスワード(privileged_password) は認証方式が Basic/Digest の場合のユーザーパスワード
自動入力の場合は ${privileged_password} を参照することでこれが入力できるです。
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 自動入力手順(browser_steps)
| データ型|object|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 初期URL(initial_url)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
## 強制切断済みユーザ(disabledUser)
クラスのキー属性はuid です。
このクラスの属性は以下のとおりです。
### UID(uid)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
## IAMポリシー(iamPolicy)
クラスのキー属性はname です。
このクラスの属性は以下のとおりです。
### ポリシー名(name)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:o:|
| 最大長|128|
### 説明(description)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
| 最大長|1000|
### 参照可能チーム(viewTeam)
| データ型|stringの配列|
|----|----------------|
|必須|:o:|
|一意|:x:|
## 連携システムオブジェクト(linkageSystem)
クラスのキー属性はsystemId です。
このクラスの属性は以下のとおりです。
### システムID(systemId)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 鍵(key)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
## SAML属性定義(samlattr)
このクラスは内包オブジェクトです。
このクラスの属性は以下のとおりです。
### 属性定義ID(attr_id)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 属性(attr)
属性(attr) はE/U管理者画面には「連携項目名」として表示です。
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
## SAML NameID定義(samlnameid)
このクラスは内包オブジェクトです。
このクラスの属性は以下のとおりです。
### NameIDフォーマット(nameid_format)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### SAML属性ID(attr_id)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
## SAML SP(samlsp)
このクラスは内包オブジェクトです。
このクラスの属性は以下のとおりです。
### 公開名(published_name)
公開名(published_name) はSPのシステムを一位に特定するための名称です。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### SP メタデータ(metadata)
SP メタデータ(metadata) はSAML  SP のメタデータです。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### サービスURL(service_url)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 配信対象SAML属性(saml_attrs_for_delivery)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### SAMLアサーションを暗号化する(encryptAssertions)
SAMLアサーションを暗号化する(encryptAssertions) はIdPからSPに送るSAMLアサーションを暗号化する。 Office 365の場合は false を設定する。です。
| データ型|boolean|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 応答に署名する(signResponses)
応答に署名する(signResponses) はIdPからSPに送る応答に署名する。Office365の場合は  false を指定する。です。
| データ型|boolean|
|----|----------------|
|必須|:x:|
|一意|:x:|
### SAMLアサーションに署名する(signAssertions)
SAMLアサーションに署名する(signAssertions) はIdPからSPに送るSAMLアサーションに署名する。Office365の場合は true を指定する。です。
| データ型|boolean|
|----|----------------|
|必須|:x:|
|一意|:x:|
## 利用者(user)
クラスのキー属性はuid です。
このクラスの属性は以下のとおりです。
### ユーザID(uid)
ユーザID(uid) はユーザを識別する一意の IDです。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:o:（大文字小文字無視）|
| 最大長|64|
| 最小長|1|
### 姓(sn)
姓(sn) はユーザの姓です。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
| 最大長|256|
| 最小長|1|
### 名(cn)
名(cn) はユーザの名です。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
| 最大長|256|
| 最小長|1|
### ID管理役割(idmRole)
ID管理役割(idmRole) はユーザがID管理業務上割り当てられた役割です。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 初期パスワード(userPassword)
初期パスワード(userPassword) はユーザのパスワードです。
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### チーム(team)
| データ型|stringの配列|
|----|----------------|
|必須|:o:|
|一意|:x:|
### アカウント無効化(accountLock)
アカウント無効化(accountLock) はtrue の場合アカウントを無効化するです。
| データ型|boolean|
|----|----------------|
|必須|:x:|
|一意|:x:|
### メールアドレス(email)
メールアドレス(email) は現行運用のメールアドレス
※委託社員等は本人とは限らないです。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 表示名(displayName)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 説明(description)
説明(description) はユーザの説明です。
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 初回ログイン日時(loginDateTime)
初回ログイン日時(loginDateTime) は初回ログインが行われた日時です。
| データ型|datetime|
|----|----------------|
|必須|:x:|
|一意|:x:|
## チーム(userGroups)
クラスのキー属性はid です。
このクラスの属性は以下のとおりです。
### チームID(id)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:o:|
### チーム名(name)
チーム名(name) はチーム名を入力してください。です。
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:o:|
### ユーザー一覧(users)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 管理者(managers)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### アカウントロック通知(accountLockNotification)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 全踏み台ログインログアウト通知(即時)(immediateLoginLogoutNotification)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 全踏み台ログインログアウト通知(サマリ)(summaryLoginLogoutNotification)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 事前認証実施通知(即時)(immediatePreapprovalNotification)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 事前認証実施通知(サマリ)(summaryPreapprovalNotification)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 許可アドレスリスト(allowedAddressList)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 禁止アドレスリスト(forbiddenAddressList)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
## 作業(work)
クラスのキー属性はid です。
このクラスの属性は以下のとおりです。
### 業務ID(id)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:o:|
### 業務名(name)
| データ型|string|
|----|----------------|
|必須|:o:|
|一意|:o:|
### 作業期間(Periods)
| データ型|objectの配列|
|----|----------------|
|必須|:o:|
|一意|:x:|
### ステータス(status)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 接続先(connections)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 接続先グループ(connectionGroups)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 接続先一覧(resultConnections)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 作業者（単体選択）(workUsers)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 作業者（チーム選択）(workUserGroups)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 作業者一覧(resultUsers)
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 管理者（単体選択）(manageUsers)
| データ型|stringの配列|
|----|----------------|
|必須|:o:|
|一意|:x:|
### 申請者(applicationUser)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 申請日時(applicationDatetime)
| データ型|datetime|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 申請者コメント(applicationComment)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 申請者メールリスト(applicationMailList)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 承認者(approvedUser)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 承認日時(approvedDatetime)
| データ型|datetime|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 承認者コメント(approvedComment)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 承認要求日時(approveRequestDatetime)
| データ型|datetime|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 承認完了日時(approveCompletionDate)
| データ型|datetime|
|----|----------------|
|必須|:x:|
|一意|:x:|
### 承認者メールリスト(approveMailList)
| データ型|string|
|----|----------------|
|必須|:x:|
|一意|:x:|
### IAMポリシー(iamPolicies)
IAMポリシー(iamPolicies) はAWS IAMポリシーを選択するです。
| データ型|stringの配列|
|----|----------------|
|必須|:x:|
|一意|:x:|
